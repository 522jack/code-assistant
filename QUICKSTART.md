# Code Assistant - Быстрый старт

## 🎯 Что это?

**code-assistant** - универсальный CLI инструмент для разработчиков, который отвечает на вопросы о структуре любого проекта, используя RAG (Retrieval-Augmented Generation).

## ✅ Готовый результат

Проект **полностью реализован** со следующим функционалом:

- ✅ **RAG индексация**: Автоматическая индексация README + папка `docs/` (или `project/docs/`)
- ✅ **Универсальность**: Работает с любым git-проектом
- ✅ **Git MCP**: Получение текущей ветки через команду `/branch`
- ✅ **Команда /help**: Ответы на вопросы о проекте с использованием RAG
- ✅ **Кеширование**: Индексы сохраняются в `~/.code-assistant/` для каждого проекта
- ✅ **Автоопределение**: Автоматически находит git root и сканирует документацию

## 📋 Требования

1. **Java 17+** (JDK или JRE)
2. **Ollama** с моделью `nomic-embed-text` (для RAG embeddings)
3. **Claude API key** (для LLM ответов)
4. **Git** (для получения информации о ветке)

## 🚀 Установка зависимостей

### 1. Установите Ollama

```bash
# macOS/Linux
curl -fsSL https://ollama.ai/install.sh | sh

# Или скачайте с https://ollama.ai
```

### 2. Загрузите модель для embeddings

```bash
ollama pull nomic-embed-text
```

### 3. Настройте переменные окружения

```bash
# Добавьте в ~/.bashrc или ~/.zshrc
export CLAUDE_API_KEY='your-api-key-here'
export OLLAMA_URL='http://localhost:11434'  # опционально, это значение по умолчанию
```

Или используйте скрипт:

```bash
./scripts/setup-env.sh
source .env.code-assistant
```

## 📦 Сборка проекта

```bash
cd /Users/evgeniy/Desktop/Projects/code-assistant

# Сборка JAR
./gradlew shadowJar
```

Результат: `build/libs/code-assistant-0.1.0.jar` (8.7 MB)

## 🎮 Использование

### Вариант 1: Запуск через скрипт

```bash
# Из директории code-assistant
./scripts/run.sh

# Или из любого проекта
cd ~/my-other-project
/Users/evgeniy/Desktop/Projects/code-assistant/scripts/run.sh
```

### Вариант 2: Прямой запуск JAR

```bash
cd ~/your-project
java -jar /Users/evgeniy/Desktop/Projects/code-assistant/build/libs/code-assistant-0.1.0.jar
```

### Вариант 3: Глобальная установка

```bash
cd /Users/evgeniy/Desktop/Projects/code-assistant
./scripts/install.sh

# Теперь можно запускать из любого проекта
cd ~/any-project
code-assistant
```

## 🎯 Команды

После запуска вы увидите REPL с доступными командами:

```
╔════════════════════════════════════════════════════════════════╗
║                    Code Assistant v0.1.0                       ║
║              AI-powered project documentation helper           ║
╚════════════════════════════════════════════════════════════════╝

Commands:
  /help [question]  - Ask a question about the project
  /branch           - Show current git branch
  /reindex          - Reindex project documentation
  /info             - Show project information
  /clear            - Clear conversation history
  /exit, /quit      - Exit the assistant

>
```

## 💡 Примеры использования

### Пример 1: Вопрос о структуре проекта

```bash
> /help Как устроена архитектура этого проекта?

🤔 Thinking...

💡 Answer:

Проект использует Clean Architecture с разделением на слои:
1. **data/** - работа с данными (API клиенты, хранилище)
2. **domain/** - бизнес-логика (менеджеры, сервисы)
3. **presentation/** - UI компоненты

Основные компоненты:
- RagService (domain/service/) - индексация и поиск документов
- ProjectDetector (project/) - автоопределение проекта
...

📚 Sources:
  • README (README.md) - similarity: 0.87
  • Architecture (docs/architecture.md) - similarity: 0.82
```

### Пример 2: Git информация

```bash
> /branch

🌿 Current branch: master
```

### Пример 3: Информация о проекте

```bash
> /info

📋 Project Information:
  Name: code-assistant
  Path: /Users/evgeniy/Desktop/Projects/code-assistant
  Hash: a3f7b9e2c1d4f8a6
  Remote: none
  Documents: 1

  Indexed documents: 1
  Indexed chunks: 24
  Last updated: 2025-12-01 16:50:35
  Index size: 145 KB
```

## 🔄 Тестирование на другом проекте

```bash
# Перейдите в любой другой git проект
cd /Users/evgeniy/Desktop/Projects/SampleAiAgentApp

# Запустите code-assistant
java -jar /Users/evgeniy/Desktop/Projects/code-assistant/build/libs/code-assistant-0.1.0.jar

# Или через скрипт
/Users/evgeniy/Desktop/Projects/code-assistant/scripts/run.sh

# Ассистент автоматически:
# 1. Определит git root
# 2. Найдет README.md и docs/
# 3. Проиндексирует документацию
# 4. Сохранит индекс в ~/.code-assistant/indexes/{project-hash}/
```

## 📂 Структура кеша

```
~/.code-assistant/
├── indexes/
│   ├── a3f7b9e2c1d4f8a6/        # code-assistant project
│   │   └── rag_index.json
│   ├── b4c8d1e3f2a5b9c7/        # SampleAiAgentApp project
│   │   └── rag_index.json
│   └── ...
└── code-assistant.log            # Логи
```

## 🎓 Что реализовано

### ✅ Основной функционал

- [x] Автоопределение git проекта (ProjectDetector)
- [x] Сканирование README.md и docs/ (DocumentationScanner)
- [x] RAG индексация с Ollama (RagService + OllamaClient)
- [x] Векторный поиск с cosine similarity
- [x] LLM ответы через Claude API (ClaudeClient)
- [x] Кеширование индексов по projectHash (IndexManager)
- [x] Git MCP tool для получения текущей ветки
- [x] CLI REPL с JLine3 (история команд, редактирование)
- [x] Логирование (kotlin-logging + logback)

### ✅ Команды

- [x] `/help [question]` - вопросы о проекте с RAG
- [x] `/branch` - текущая git ветка
- [x] `/reindex` - переиндексация документации
- [x] `/info` - информация о проекте и индексе
- [x] `/clear` - очистка истории разговора
- [x] `/exit`, `/quit` - выход

### ✅ Технологии

- Kotlin 2.0.21 + Coroutines
- Ktor 3.0.1 (HTTP client)
- kotlinx.serialization (JSON)
- JLine3 (CLI REPL)
- Ollama (embeddings)
- Claude Sonnet 4.5 (LLM)

## 🐛 Troubleshooting

### Ollama не запущен

```
❌ Error searching documentation: Cannot connect to Ollama at http://localhost:11434
```

**Решение**: Запустите Ollama
```bash
ollama serve
```

### Модель не установлена

```
❌ Error: Please start Ollama and ensure the 'nomic-embed-text' model is installed
```

**Решение**: Установите модель
```bash
ollama pull nomic-embed-text
```

### CLAUDE_API_KEY не установлен

```
❌ Error: CLAUDE_API_KEY environment variable is required
```

**Решение**: Установите переменную окружения
```bash
export CLAUDE_API_KEY='your-api-key-here'
```

### Не git репозиторий

```
❌ Error: Not a git repository. Please run code-assistant from within a git project.
```

**Решение**: Запустите из директории git проекта или инициализируйте git
```bash
git init
```

## 📊 Статус проекта

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Структура проекта | ✅ | Gradle + Kotlin + JVM |
| RAG модели | ✅ | RagModels, RagService, TextChunker |
| Ollama клиент | ✅ | Embeddings генерация |
| Claude клиент | ✅ | LLM ответы |
| ProjectDetector | ✅ | Git root + docs сканирование |
| IndexManager | ✅ | Кеширование индексов |
| Git tool | ✅ | Получение текущей ветки |
| CLI интерфейс | ✅ | REPL с JLine3 |
| Сборка | ✅ | shadowJar (8.7 MB) |
| Документация | ✅ | README + QUICKSTART |

## 🎉 Готово!

Проект **code-assistant** полностью реализован и готов к использованию!

Вы можете:
1. Запустить его в текущем проекте (code-assistant)
2. Протестировать на другом проекте (SampleAiAgentApp)
3. Установить глобально и использовать везде

**Примеры вопросов для тестирования:**
- "Какие основные компоненты есть в проекте?"
- "Как устроена RAG индексация?"
- "Где находится конфигурация приложения?"
- "Какие команды доступны в CLI?"