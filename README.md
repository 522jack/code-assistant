# Code Assistant

Универсальный CLI инструмент для помощи разработчикам с вопросами о структуре проекта.

## Возможности

- 🔍 **RAG (Retrieval-Augmented Generation)**: Индексирует README и папку `docs/` вашего проекта
- 🌳 **Git интеграция**: Получает информацию о текущей ветке через MCP
- 💬 **Команда /help**: Отвечает на вопросы о структуре и архитектуре проекта
- 🤖 **AI Code Review**: Автоматический code review через GitHub Actions с использованием RAG и Claude API
- 🚀 **Универсальность**: Работает с любым проектом, запускается из любой директории
- ⚙️ **CI/CD интеграция**: Триггер GitHub Actions workflows напрямую из CLI
- 📱 **Telegram уведомления**: Автоматические уведомления о статусе сборки с метриками и артефактами
- 📊 **Управление задачами**: Создание, отслеживание и AI-рекомендации для задач разработки

## Требования

**Обязательные:**
- Java 17 или выше
- [Ollama](https://ollama.ai) с моделью `nomic-embed-text` (для RAG embeddings)
- Git (для получения информации о ветке)
- API ключ Claude (для LLM)

**Опциональные (для дополнительных функций):**
- GitHub Personal Access Token (для CI/CD триггеров и code review)
- Telegram Bot Token и Chat ID (для уведомлений)
- [GitHub CLI (gh)](https://cli.github.com/) (для code review функций)

## Установка

### 1. Установите Ollama и загрузите модель

```bash
# Установите Ollama (если еще не установлен)
# https://ollama.ai

# Загрузите модель для embeddings
ollama pull nomic-embed-text
```

### 2. Соберите проект

```bash
./gradlew shadowJar
```

### 3. Установите в PATH (опционально)

```bash
./scripts/install.sh
```

## Использование

### Запуск в любом проекте

```bash
cd ~/your-project
code-assistant
```

### Команды

**Основные:**
- `/help [вопрос]` - задать вопрос о проекте
- `/branch` - показать текущую git ветку
- `/reindex` - переиндексировать документацию проекта
- `/info` - показать информацию о проекте
- `/clear` - очистить историю разговора
- `/exit` или `/quit` - выход

**Управление задачами:**
- `/task create [название]` - создать новую задачу
- `/tasks [фильтр]` - список задач (all, high, medium, low, todo, done)
- `/task [id]` - показать детали задачи
- `/task done [id]` - отметить задачу как выполненную
- `/recommend` - получить AI рекомендации по задачам

**CI/CD и уведомления:**
- `/build [owner/repo] [workflow] [branch]` - запустить GitHub Actions workflow
- `/status [owner/repo] [run-id]` - проверить статус сборки
- `/notify [сообщение]` - отправить уведомление в Telegram

### Примеры

```bash
# Запустить ассистента
$ code-assistant

# Основные команды
> /help Как устроена архитектура проекта?
> /help Где находится реализация аутентификации?
> /branch
> /info

# Управление задачами
> /task create Рефакторинг модуля аутентификации
> /tasks todo
> /recommend

# CI/CD и уведомления
> /build myorg/myrepo build.yml main
> /status myorg/myrepo 123456789
> /notify Build completed successfully!

> /exit
```

## Конфигурация

Создайте файл `.code-assistant.yml` в корне вашего проекта (опционально):

```yaml
docs_paths:
  - README.md
  - docs/
  - project/docs/

exclude_patterns:
  - "*.tmp"
  - ".git"

ollama_url: "http://localhost:11434"
```

Или используйте переменные окружения:

```bash
# Обязательные
export CLAUDE_API_KEY="your-api-key"
export OLLAMA_URL="http://localhost:11434"

# Опциональные (для CI/CD и уведомлений)
export GITHUB_TOKEN="ghp_xxxxxxxxxxxxx"           # Personal Access Token с правами repo
export TELEGRAM_BOT_TOKEN="123456:ABC-DEF..."      # Bot token от @BotFather
export TELEGRAM_CHAT_ID="-1001234567890"           # ID чата или канала
```

## Архитектура

```
code-assistant/
├── cli/                    # CLI интерфейс с REPL
├── project/                # Детектор проекта и сканер документации
├── index/                  # Управление индексами (~/.code-assistant/)
├── rag/                    # RAG: эмбеддинги, векторный поиск
├── llm/                    # Claude и Ollama клиенты
├── git/                    # Git инструменты
├── github/                 # GitHub интеграция (PR, code review)
├── cicd/                   # CI/CD триггеры (GitHub Actions)
├── notifications/          # Telegram Bot API клиент
├── task/                   # Система управления задачами
├── review/                 # AI Code Review сервис
└── config/                 # Конфигурация
```

## Как это работает

1. **Автоопределение проекта**: Находит git root и сканирует README + docs/
2. **Индексация**: Создает векторные эмбеддинги через Ollama (nomic-embed-text)
3. **Кеширование**: Сохраняет индексы в `~/.code-assistant/indexes/{project-hash}/`
4. **RAG поиск**: При вопросе находит релевантные фрагменты документации
5. **LLM ответ**: Claude генерирует ответ на основе найденных фрагментов

## Разработка

### Структура проекта

```
src/main/kotlin/com/codeassistant/
├── Main.kt                          # Entry point
├── cli/                             # CLI интерфейс
├── project/                         # Детектор проекта
├── index/                           # Управление индексами
├── rag/                             # RAG логика
├── llm/                             # LLM клиенты
├── git/                             # Git инструменты
└── config/                          # Конфигурация
```

### Сборка

```bash
# Обычная сборка
./gradlew build

# Fat JAR со всеми зависимостями


# Запуск
java -jar build/libs/code-assistant-0.1.0.jar
```

## AI Code Review в GitHub Actions

Настройка автоматического code review для Pull Requests:

### 1. Добавьте секрет в GitHub

В настройках репозитория добавьте секрет:
- `CLAUDE_API_KEY` - ваш API ключ Claude

### 2. Создайте Pull Request

GitHub Action запустится автоматически при комментарии `/review` в PR.

### 3. Получите результат

Ассистент:
1. Проиндексирует документацию и код проекта через RAG
2. Получит diff PR через GitHub CLI
3. Проанализирует изменения с использованием контекста проекта
4. Отправит structured markdown комментарий с:
   - Сводкой изменений
   - Проблемами по категориям (Bug, Security, Performance, etc.)
   - Уровнями важности (Critical, High, Medium, Low)
   - Конкретными советами по улучшению
   - Позитивными аспектами
   - Общей оценкой

### Пример использования

```bash
# В комментарии PR напишите:
/review

# GitHub Action автоматически запустится и через несколько минут
# в PR появится подробный code review от AI
```

## CI/CD Интеграция и Telegram Уведомления

### Настройка GitHub Actions триггера

#### 1. Добавьте workflow в ваш проект

В репозитории, который вы хотите собирать, создайте файл `.github/workflows/build.yml`:

```yaml
name: Build and Deploy

on:
  # Разрешить запуск через API (для Code Assistant)
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: false
        default: 'staging'

  # Автоматический запуск при push
  push:
    branches: [ main, develop ]

jobs:
  build:
    name: Build Project
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Build fat JAR
        run: ./gradlew shadowJar

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: build/libs/*.jar
          retention-days: 30
```

**Важно:** Секция `workflow_dispatch` обязательна для запуска через API!

Для других типов проектов замените шаги сборки:
- **Node.js**: `npm install && npm run build`
- **Python**: `pip install -r requirements.txt && python setup.py build`
- **Go**: `go build -o app ./cmd/main.go`
- **Docker**: `docker build -t myapp:latest .`

#### 2. Создайте Personal Access Token

В настройках GitHub (Settings → Developer settings → Personal access tokens):
- Создайте новый token с правами:
  - `repo` - полный доступ к репозиториям
  - `workflow` - возможность запуска workflows

```bash
export GITHUB_TOKEN="ghp_xxxxxxxxxxxxx"
```

#### 3. Запуск сборки через CLI

```bash
# Запустить workflow для репозитория
$ code-assistant
> /build myorg/myrepo build.yml main

🚀 Triggering build for myorg/myrepo...
   Workflow: build.yml
   Branch: main

✅ Workflow triggered successfully - https://github.com/myorg/myrepo/actions/runs/123456

Use /status myorg/myrepo 123456 to check status
```

#### 4. Проверка статуса сборки

```bash
> /status myorg/myrepo 123456

📊 Build Status
   Status: completed
   Conclusion: ✅ success
   URL: https://github.com/myorg/myrepo/actions/runs/123456
   Duration: 5m 32s

📦 Artifacts (2)
   - app-release.apk (45.23 MB)
   - test-results.zip (1.15 MB)
```

### Настройка Telegram уведомлений

#### 1. Создайте Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot` и следуйте инструкциям
3. Сохраните токен бота (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### 2. Получите Chat ID

Вариант 1 - для личного чата:
1. Напишите вашему боту любое сообщение
2. Откройте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите `"chat":{"id":123456789}` в ответе

Вариант 2 - для группы/канала:
1. Добавьте бота в группу/канал
2. Сделайте его администратором (для каналов)
3. Отправьте любое сообщение
4. Используйте метод `getUpdates` как выше
5. ID группы/канала начинается с `-100` (например: `-1001234567890`)

#### 3. Настройте переменные окружения

```bash
export TELEGRAM_BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
export TELEGRAM_CHAT_ID="-1001234567890"  # или ваш личный chat ID
```

#### 4. Используйте уведомления

**Автоматические уведомления при сборке:**
```bash
> /build myorg/myrepo build.yml main

# Если настроен Telegram, вы автоматически получите уведомления:
# 1. ⏳ Сборка запущена
# 2. ✅/❌ Результат сборки с метриками, логами ошибок и артефактами
```

**Ручные уведомления:**
```bash
> /notify Deployment completed to production! 🚀

📤 Sending notification to Telegram...
✅ Notification sent successfully!
```

### Формат уведомлений о сборке

Telegram уведомление включает:

✅ **myproject** - Success

📦 **Branch:** `main`
🔖 **Commit:** `abc12345`
👤 **Author:** John Doe
💬 **Message:** Fix authentication bug

⏱ **Duration:** 5m 32s

📊 **Metrics:**
  • Tests: 142/145 passed (3 failed)
  • Coverage: 87.5%
  • Artifact size: 45.23 MB

🔗 [View Build Details](https://github.com/myorg/myrepo/actions/runs/123456)

При успешной сборке также отправляется файл артефакта (если доступен).

## Управление Задачами

Встроенная система задач с AI-рекомендациями:

```bash
# Создать задачу
> /task create Рефакторинг модуля аутентификации

# Просмотр задач
> /tasks todo              # Только незавершенные
> /tasks high              # Высокий приоритет
> /tasks done              # Завершенные

# Детали задачи
> /task abc12345

# Завершить задачу
> /task done abc12345

# AI рекомендации
> /recommend
💡 Recommended Tasks (Top 3)

1. [abc12345] Рефакторинг модуля аутентификации (HIGH)
   Score: 8.5/10

   Reasoning: Эта задача критична для безопасности...
```

## Roadmap

- [x] Базовая RAG индексация (README + docs/)
- [x] Git branch через MCP
- [x] Команда /help
- [x] AI Code Review через GitHub Actions
- [x] Индексация исходного кода (не только документации)
- [x] Управление задачами с AI-рекомендациями
- [x] CI/CD интеграция (GitHub Actions триггер)
- [x] Telegram уведомления о сборках
- [x] Отправка артефактов в Telegram
- [ ] Дополнительные git команды
- [ ] Поддержка других CI/CD систем (GitLab CI, Jenkins)
- [ ] Интеграция с IDE
- [ ] Web интерфейс

## Лицензия

MIT

## Авторы

Создано с использованием:
- [Claude API](https://www.anthropic.com/claude)
- [Ollama](https://ollama.ai)
- Kotlin + Ktor